# PHASE 1: Gradle wrapper will be added when a real Android Studio / Gradle environment runs
# `gradle wrapper` for the first time. PHASE 1 keeps the wrapper ungenerated so the
# customer can choose their Gradle distribution.
# Once the wrapper is generated, this directory will contain:
#   gradle-wrapper.jar
#   gradle-wrapper.properties